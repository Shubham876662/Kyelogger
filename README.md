# Kyelogger
Python program that captures keyboard keystrokes and logs them into a .txt file along with timestamps. The script was converted into a standalone Windows .exe to allow execution without a Python environment, focusing on learning system-level input handling and deployment.
